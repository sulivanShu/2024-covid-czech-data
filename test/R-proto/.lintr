linters: linters_with_defaults(
						 line_length_linter(60),
						 commented_code_linter = NULL,
						 assignment_linter = NULL,     # autorise '->' et '='
						 line_length_linter = NULL,    # désactive l'avertissement sur la longueur de ligne
						 commented_code_linter = NULL, # ne signale pas le code commenté
						 object_length_linter = NULL,  # pas de limite sur la longueur des noms de variables
						 object_usage_linter = NULL,    # ne signale pas les variables non utilisées
						 indentation_linter = NULL
						 )
